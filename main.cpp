#include<iostream>
#include "searchingAlgorythms.hpp"
#include "sortingAlgorythms.hpp"



int main() {
    constexpr int size = 5;
    int tab[size] = {3,7,1,5,9};
   // int key_1 = 7;    /* klucze uzywac tylko do alg. wyszukiwania*/
   // int key_2 = 21;



    //bubbleSort(tab, size-1);
    //for (int i = 0; i < size; i++){
    //   std::cout << " " << tab[i];
//  };



    //cocktailSort(tab, size-1);
    // for (int i = 0; i < size; i++){
    //    std::cout << " " << tab[i];
    //  };



    // insertionSort(tab, size-1);
    //  for (int i = 0; i < size; i++){
    //      std::cout << " " << tab[i];
//    };



   // mergeSort(tab, 0, size-1);
   // for (int i = 0; i < size; i++){
   //     std::cout << " " << tab[i];
  //  };



//       quickSort(tab, 0, size-1);
//    for (int i = 0; i < size; i++){
 //       std::cout << " " << tab[i];
//   };


    //int result = sentinelSearch(tab, size, key_1);
    //std::cout << "W tablicy klucz o wartosci: " << key_1 << "Znajduje sie pod indeksem: "
    //<< result << std::endl;
   // result = sentinelSearch(tab, size, key_2);
   // std::cout << "W tablicy klucz o wartosci: " << key_2 << "Znajduje sie pod indeksem: "
   // << result << std::endl;



   // int result = linear(tab, size, key_1);
   // std::cout << "W tablicy klucz o wartosci: " << key_1 << "Znajduje sie pod indeksem: "
   // << result << std::endl;
   // result = linear(tab, size, key_2);
   // std::cout << "W tablicy klucz o wartosci: " << key_2 << "Znajduje sie pod indeksem: "
   // << result << std::endl;


    //int result = searchJump(tab,  size, key_1);
   // std::cout << "W tablicy klucz o wartosci: " << key_1 << "Znajduje sie pod indeksem: "
   // << result << std::endl;
   // result = searchJump(tab, size, key_2);
  // std::cout << "W tablicy klucz o wartosci: " << key_2 << "Znajduje sie pod indeksem: "
  //  << result << std::endl;



//    int result = interpolationSearch(tab, 0, size, key_1);
//    std::cout << "W tablicy klucz o wartosci: " << key_1 << "Znajduje sie pod indeksem: "
//    << result << std::endl;
//    result = interpolationSearch(tab, 0, size, key_2);
//    std::cout << "W tablicy klucz o wartosci: " << key_2 << "Znajduje sie pod indeksem: "
//    << result << std::endl;


 //  int result = binarySearch(tab, 0, size, key_1);
 //  std::cout << "W tablicy klucz o wartosci: " << key_1 << "Znajduje sie pod indeksem"
  //  << result << std::endl;
  //  result = binarySearch(tab, 0, size, key_2);
  //  std::cout << "W tablicy klucz o wartosci: " << key_2 << "Znajduje sie pod indeksem"
    //          << result << std::endl;


    
    return 0;
}